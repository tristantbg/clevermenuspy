# MenuSpy - Vanilla javascript Intersection Observer menu active class setter

```javascript
window.onload = function () {

    const MenuSpy = new MenuSpy('header', {activeClass: 'active'});
};

```

## Install

```sh
$ npm install clevermenuspy --save
```
